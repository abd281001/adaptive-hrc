# Qualified RTX 50-series inference stack for the frozen Qwen baseline.
# PyTorch 2.13 ships the stable CUDA 13 wheel on PyPI. bitsandbytes 0.50.x's
# fused nested-NF4 path is intentionally excluded: it is not viable for this
# Qwen checkpoint on the RTX 5070. Runtime preflight enforces these exact pins.
torch==2.13.0
transformers==5.16.1
accelerate==1.14.0
bitsandbytes==0.49.2
certifi==2026.7.22
idna==3.19
